### Инструкция по запуску

### Запуск всех тестов
python -m unittest discover

### Запуск конкретных тестовых модулей
python -m unittest test_new_features.py
python -m unittest test_filemanager.py

### Запуск с подробным выводом
python -m unittest -v test_new_features.py

### Запуск конкретного теста
python -m unittest test_new_features.TestBankAccountJSON.test_load_bank_data_with_valid_json

### 📝 Что нового в этой версии:
#### 1. Банковский счет с JSON сохранением:

    ✅ Сохранение баланса в JSON файл

    ✅ Сохранение истории покупок с деталями (дата, сумма, остаток)

    ✅ Автоматическая загрузка при старте

    ✅ Очистка истории с подтверждением

    ✅ JSON формат для лучшей структурированности данных

#### 2. Сохранение содержимого директории:

    ✅ Создание файла listdir.txt

    ✅ Перезапись существующего файла

    ✅ Формат: сначала все файлы, потом все папки

    ✅ Корректная обработка пустых директорий

#### 3. Новые тесты:

    ✅ 10+ тестов для JSON банковского счета

    ✅ 8+ тестов для сохранения директории

    ✅ Интеграционные тесты

    ✅ Тесты с моками для ошибок

#### Пример содержимого listdir.txt:
text

files:
victory.py
bill.py
main.py
test.txt

dirs:
modules
packages
images
